# Django
SECRET_KEY=change-this-with-a-strong-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Upload limits (MB)
# Preferred key used by settings.py
MAX_UPLOAD_BYTES_MB=20
RATE_LIMIT_PER_MINUTE=10

# Legacy alias still supported by settings.py (optional)
# MAX_UPLOAD_SIZE_MB=20
